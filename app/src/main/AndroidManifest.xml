<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.implude.niche">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.NicheApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Niche">
        <activity
            android:name=".presentation.main.user.MyCommentsActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.main.user.MyHeartsActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.CertificateActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.EditStoreInfoActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.StoreDetailActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>